# Copy to .dev.vars for local Pages development.
SESSION_SECRET=replace-with-a-long-random-secret
# Optional. When set, the first administrator must provide this token during initial setup.
BOOTSTRAP_TOKEN=
