# Copy to .dev.vars for local Worker development.
# Generate with: openssl rand -base64 48
SESSION_SECRET=replace-with-a-long-random-secret

# Optional. When set, the first administrator must provide this token.
BOOTSTRAP_TOKEN=
